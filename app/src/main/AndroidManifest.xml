<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notero">

        <!-- NoteroApplication as Launcher Activity -->
        <activity
            android:name=".NoteroApplication"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Notero"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WelcomeActivity for onboarding -->
        <activity
            android:name=".ui.welcome.WelcomeActivity"
            android:theme="@style/Theme.Notero" />

        <!-- MainActivity for the standard app interface -->
        <activity
            android:name=".ui.overview.MainActivity"
            android:theme="@style/Theme.Notero" />

        <!-- Other activities -->
        <activity android:name=".ui.period.PeriodHistoryActivity" />
        <activity android:name=".ui.editor.EntryEditorActivity" />
        <activity android:name=".ui.detail.EntryDetailActivity" />
    </application>

</manifest>